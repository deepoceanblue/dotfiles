# Generated by iptables-save v1.8.4 on Sat Feb  1 23:05:16 2020
*filter
# we default to DROP
:INPUT DROP [0:0]
:FORWARD DROP [0:0]
# optionally loaded ruleset may change this to DROP later
:OUTPUT ACCEPT [0:0]
# new chains
:TCP - [0:0]
:UDP - [0:0]
# trust responses
-A INPUT -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
# loopback
-A INPUT -i lo -j ACCEPT
# change to DROP if you don't care for ICMPv6 Neighbouring Discovery packets  
-A INPUT -p ipv6 -j ACCEPT
# drop all invalid packets
-A INPUT -m conntrack --ctstate INVALID -j DROP
# allow ECHO REQUEST---blocking/silently dropping creates more trouble than it is worth
-A INPUT -p icmp -m icmp --icmp-type 8 -m conntrack --ctstate NEW -j ACCEPT
# send to UDP where these will be properly rejected
-A INPUT -p udp -m conntrack --ctstate NEW -j UDP
# as above for TCP
-A INPUT -p tcp -m tcp --tcp-flags FIN,SYN,RST,ACK SYN -m conntrack --ctstate NEW -j TCP
# imitate default linux behaviour
-A INPUT -p udp -j REJECT --reject-with icmp-port-unreachable
-A INPUT -p tcp -m recent --set --name TCP-PORTSCAN --mask 255.255.255.255 --rsource -j REJECT --reject-with tcp-reset
-A INPUT -p udp -m recent --set --name UDP-PORTSCAN --mask 255.255.255.255 --rsource -j REJECT --reject-with icmp-port-unreachable
# imitate default linux behaviour
-A INPUT -j REJECT --reject-with icmp-proto-unreachable
# every host that is found to be in *-PORTSCAN in the last 90seconds
-A TCP -p tcp -m recent --update --seconds 90 --name TCP-PORTSCAN --mask 255.255.255.255 --rsource -j REJECT --reject-with tcp-reset
-A TCP -p tcp -j REJECT --reject-with tcp-reset
# imitate default linux behaviour
-A UDP -p udp -j REJECT --reject-with icmp-port-unreachable
COMMIT
# Completed on Sat Feb  1 23:05:16 2020
